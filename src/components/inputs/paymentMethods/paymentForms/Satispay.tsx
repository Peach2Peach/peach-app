import React from 'react'
import { FormProps } from './PaymentMethodForm'
import { Template3 } from '../templates'

export const Satispay = (formProps: FormProps) => <Template3 {...formProps} name="satispay" />
