import React from 'react'
import { Template3 } from '../templates'
import { FormProps } from './PaymentMethodForm'

export const MobilePay = (formProps: FormProps) => <Template3 {...formProps} name="mobilePay" />
