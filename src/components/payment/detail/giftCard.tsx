import React, { ReactElement } from 'react'
import { View } from 'react-native'
import { PaymentTemplateProps } from '..'
import { Text } from '../..'

export const DetailGiftCard = ({ paymentData }: PaymentTemplateProps): ReactElement => <View>
  <Text>TODO</Text>
</View>

export default DetailGiftCard