export const dataMigrationBeforeLoadingAccount = () => {

  /**
   * Keep as container, nothing to migrate at the moment.
   * Note: when migration relies on zustant-persist. Make sure to check
   * first for hydration status and wait for hydration to finish before
   * running data migrations on it.
   */
}
